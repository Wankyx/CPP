#ifndef GAME_HPP  
#define GAME_HPP

extern bool gameReverse(const std::string& wordReversed, const std::string& wordHiden, bool gameOver);
extern bool gameHidden(const std::string& word, bool gameOver);

#endif